{
  "name": "antfarm",
  "version": "0.5.4",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "antfarm": "dist/cli/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && cp src/server/index.html dist/server/index.html && chmod +x dist/cli/cli.js && node scripts/inject-version.js",
    "test": "npm run build && node --test dist/**/*.test.js tests/**/*.test.ts",
    "start": "node dist/cli/cli.js"
  },
  "dependencies": {
    "json5": "^2.2.3",
    "yaml": "^2.4.5"
  },
  "devDependencies": {
    "@types/node": "^25.4.0",
    "typescript": "^5.9.3"
  }
}
