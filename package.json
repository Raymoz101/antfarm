{
  "name": "antfarm",
  "version": "0.5.7",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "antfarm": "dist/cli/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && cp src/server/index.html dist/server/index.html && chmod +x dist/cli/cli.js && node scripts/inject-version.js",
    "test": "env ANTFARM_TEST=1 NODE_ENV=test npm run build && env ANTFARM_TEST=1 NODE_ENV=test node --test dist/**/*.test.js tests/**/*.test.ts",
    "start": "node dist/cli/cli.js"
  },
  "dependencies": {
    "json5": "^2.2.3",
    "yaml": "^2.4.5"
  },
  "devDependencies": {
    "@types/node": "^25.4.0",
    "typescript": "^5.9.3"
  }
}
